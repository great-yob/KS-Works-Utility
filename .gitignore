# Dependencies
node_modules/

# Build output (regenerated by `npm run build` / `build:exe`)
build/
dist/
release/
coverage/

# Native binary download/extraction artifact (re-created by download_gs.mjs)
gs_installer.exe

# Python worker build intermediates
__pycache__/
*.pyc

# Environment / secrets
.env*
!.env.example

# Logs
*.log
npm-debug.log*

# OS / editor / local tool state
.DS_Store
Thumbs.db
.claude/settings.local.json
.antigravity/
