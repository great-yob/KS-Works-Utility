# Dependencies
node_modules/

# Build output (regenerated by `npm run build` / `build:exe`)
build/
dist/
release*/
coverage/

# Native binary download/extraction artifact (re-created by download_gs.mjs)
gs_installer.exe

# Python worker build intermediates
__pycache__/
*.pyc

# Environment / secrets (build machine only — never commit)
.env*
!.env.example
*.local.ps1

# Logs
*.log
npm-debug.log*

# OS / editor / local tool state
.DS_Store
Thumbs.db
.claude/settings.local.json
.antigravity/

# Design sources (원본 디자인 파일 — 빌드에는 assets/icon.ico 사용)
소스/
